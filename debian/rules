#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk

install/nanggar-branding-gimp::
	INSTALL_ROOT=$(CURDIR)/debian/tmp make install
	
install/nanggar-branding-openoffice::
	INSTALL_ROOT=$(CURDIR)/debian/tmp make install
